$url = "http://www.yad2.co.il/Nadlan/Neighbors/realEstateObjCreator.php";
$ch = curl_init();
curl_setopt($ch, CURLOPT_URL, $url);
// $output contains the output string 
        $output = curl_exec($ch); 
curl_close($ch);
